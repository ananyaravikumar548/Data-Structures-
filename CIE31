class graph:
    def __init__(self):
        self.adj_list ={}
    
    def add_edge(v,w):
        if u not in adj_list:
            self.adj_list(u)==[]
        if v not in adj_list:
            self.adj_list(v)==[]
        self.adj_list(u).append(v)
        self.adj_list(v).append(u)
    
